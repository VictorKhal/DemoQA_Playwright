const DROPPED = 'Dropped!';
const DROP_HERE = 'Drop here';

const OUTER_WRAPPER = 'Outer droppable';
const NOT_GREEDY_INNER = 'Inner droppable (not greedy)'; ; 
const GREEDY_INNER = 'Inner droppable (greedy)'; 

export {
    DROPPED,
    DROP_HERE,
    OUTER_WRAPPER,
    NOT_GREEDY_INNER,
    GREEDY_INNER,
}